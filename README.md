# trading-bot

Crypto trading bot with live engines and backtesting strategies.

## Run CLI

```bash
python app/main.py
```

## Run deployed web tester

This repo now serves a simple deployment-friendly web tester through `/api` (and `/` via rewrite).

- Open `/` in deployment for the HTML tester UI.
- Use `/health` for health checks.
